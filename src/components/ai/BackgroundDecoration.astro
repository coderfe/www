---
// 背景装饰组件
---

<div class="fixed inset-0 overflow-hidden -z-10">
  <div class="absolute -inset-[100%] bg-gradient-to-br from-violet-500/20 via-fuchsia-500/10 to-transparent animate-glow"></div>
  <div class="absolute -inset-[100%] bg-gradient-to-tl from-cyan-500/20 via-blue-500/10 to-transparent animate-glow-delay"></div>
  <div class="absolute top-[-20%] right-[-20%] w-[100vh] h-[100vh] rounded-full bg-fuchsia-600/20 blur-[100px] animate-pulse"></div>
  <div class="absolute bottom-[-20%] left-[-20%] w-[100vh] h-[100vh] rounded-full bg-cyan-600/20 blur-[100px] animate-pulse-delay"></div>
</div>

<style>
  @keyframes glow {
    0%, 100% { opacity: 0.3; transform: scale(0.8); }
    50% { opacity: 0.5; transform: scale(1); }
  }
  
  @keyframes pulse {
    0%, 100% { opacity: 0.2; }
    50% { opacity: 0.4; }
  }

  .animate-glow {
    animation: glow 10s ease-in-out infinite;
  }

  .animate-glow-delay {
    animation: glow 10s ease-in-out infinite;
    animation-delay: -5s;
  }

  .animate-pulse-delay {
    animation: pulse 8s ease-in-out infinite;
    animation-delay: -4s;
  }
</style>
